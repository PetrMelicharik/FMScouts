name: FMScouts Scraper

on:
  schedule:
    - cron: '0 3 * * 2'   # Úterý — hráči + zápasy
    - cron: '0 3 * * 5'   # Pátek — hráči + zápasy
    - cron: '0 4 * * 1'   # Pondělí — jen zápasy (po víkendu)
  workflow_dispatch:
    inputs:
      mode:
        description: 'Co spustit'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - players
          - matches

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Scrape players
        if: ${{ github.event.inputs.mode != 'matches' }}
        run: python scraper.py
        env:
          APIFOOTBALL_KEY: ${{ secrets.APIFOOTBALL_KEY }}

      - name: Commit player data
        if: ${{ github.event.inputs.mode != 'matches' }}
        run: |
          git config user.name "FMScouts Bot"
          git config user.email "bot@fmscouts.com"
          git add data/players.json
          git diff --staged --quiet || git commit -m "Players update $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: Scrape matches
        if: ${{ github.event.inputs.mode != 'players' }}
        run: python scraper_matches.py
        env:
          APIFOOTBALL_KEY: ${{ secrets.APIFOOTBALL_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
